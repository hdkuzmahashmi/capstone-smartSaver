import ActionButton from "../ActionButton";
import { StyledFooter } from "./Footer.styled";

function Footer() {
  return (
    <StyledFooter>
      <ActionButton />
    </StyledFooter>
  );
}

export default Footer;
